import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('smoke test', (WidgetTester tester) async {
    // App requires Supabase initialization; this is a placeholder.
    expect(true, isTrue);
  });
}
